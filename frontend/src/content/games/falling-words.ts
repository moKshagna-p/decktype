import type { GameDefinition } from '../../entities/game/types'

const fallingWordsGame: GameDefinition = {
  id: 'falling-words',
  slug: 'falling-words',
  name: 'Falling Words',
  description: 'Catch words before they hit the bottom of the screen.',
  defaultLanguage: 'en',
}

export default fallingWordsGame
