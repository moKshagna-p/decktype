{
  "name": "decktype",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"bun run --cwd apps/backend dev\" \"bun run --cwd apps/frontend dev\"",
    "cf:login": "wrangler login",
    "cf:fe:build": "bun run --cwd apps/frontend build",
    "cf:fe:deploy": "bun run cf:fe:build && wrangler pages deploy apps/frontend/dist --project-name decktype --branch main",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "sync:contributors": "bun run scripts/fetch-contributors.js",
    "lint": "oxlint",
    "typecheck": "bun run --cwd apps/backend typecheck && bun run --cwd apps/frontend typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "oxlint": "^1.15.0",
    "oxfmt": "^0.5.0",
    "typescript": "~5.9.3",
    "wrangler": "^4.86.0"
  }
}
